name: Build Vue

on: [push]

jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v3
      with:
        username: 'AhmedElshenawy0'
        reponame: 'vue-task'
        token: ${{ secrets.GITHUB_TOKEN }}
